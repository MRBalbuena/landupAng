import { Component } from '@angular/core';

@Component({
    selector: 'landup-home',
    templateUrl: '/app/home.component.html'
})
export class HomeComponent{
    title: string = "Landup Home Page";
}